import 'package:flutter/material.dart';

class AppTextStyle {
  static reusableText14(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 14, fontWeight: fontWeight, color: textColor),
    );
  }

  static reusableText16(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 16, fontWeight: fontWeight, color: textColor),
    );
  }

  static reusableText18(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 18, fontWeight: fontWeight, color: textColor),
    );
  }

  static reusableText20(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 20, fontWeight: fontWeight, color: textColor),
    );
  }

  static reusableText22(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 22, fontWeight: fontWeight, color: textColor),
    );
  }

  static reusableText24(
      {required String text,
      FontWeight fontWeight = FontWeight.normal,
      TextAlign? textAlign = TextAlign.justify,
      Color textColor = Colors.black}) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(fontSize: 24, fontWeight: fontWeight, color: textColor),
    );
  }
}
